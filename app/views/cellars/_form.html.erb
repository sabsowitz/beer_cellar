<%= form_for(@cellar) do |f| %>

  <%= render 'shared/error_messages', object: f.object %>

  <div>
    <%= f.label :title %>:
    <%= f.text_field :title %>
  </div>
  <br />
  
  <div>
    <%= f.submit 'Save' %>
  </div>

<% end %>