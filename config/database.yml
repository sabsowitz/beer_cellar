# PostgreSQL. Versions 8.2 and up are supported.

development:
  adapter: postgresql
  database: beer_cellar_development
  host: localhost

test:
  adapter: postgresql
  database: beer_cellar_test
  host: localhost

production:
  adapter: postgresql
  encoding: unicode
  database: beer_cellar_production
  pool: 5

